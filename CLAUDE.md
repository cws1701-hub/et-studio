# 프로젝트: 유럽축구 하이라이트·리뷰 SNS 자동화

인스타그램 릴스 / 네이버 블로그 / 유튜브용 유럽축구 경기 리뷰 콘텐츠를
데이터 수집 → 대본 작성 → 게시용 캡션까지 자동 생성하는 파이프라인입니다.
(기획 원본: `docs/유럽축구_SNS자동화_기획안.md`)

## 저작권 원칙 (최우선, 예외 없음)

- 방송사 하이라이트 영상·중계 화면 캡처는 **절대 다운로드/재편집하지 않는다.**
  영상이 필요하면 구단·리그 공식 채널의 **임베드/링크 공유**만 사용한다.
- 모든 영상은 자체 제작 그래픽(스탯카드, 순위표, 득점 타임라인)으로 구성한다.
- 저작권 있는 배경음악(응원가 등) 대신 무료/로열티 프리 BGM만 사용한다.
- 평점은 특정 사이트(WhoScored, SofaScore 등) 수치를 그대로 복사하지 않고,
  참고 후 자체 재해석한 점수와 근거를 붙인다.

## 커버 대상 대회

프리미어리그, 라리가, 분데스리가, 세리에A, 리그1, UEFA 챔피언스리그,
UEFA 유로파리그, 유럽 국가대표 A매치

## 톤 & 콘텐츠 규칙

- 톤: 캐스터 70% + 예능 자막 30%. "~했습니다"보다 "~했죠", "~인데요" 같은 구어체.
- 개그 요소는 **결과에 대한 유머**만 허용 (스코어를 바로 공개하지 않고 궁금증 유발,
  선수 캐릭터화, 자책골/실수는 웃음 포인트로, 패배팀 팬 시점 개그, 숫자로 임팩트).
- **선수·감독·국적/인종 관련 비하, 인신공격은 절대 금지.** "결과에 대한 유머"와
  "사람에 대한 조롱"은 다르다 — 애매하면 빼고 간다.
- 매 콘텐츠에 **MOM(자체 선정) + 평점(자체 재해석) + 리그 순위 변동** 3종을 반드시 포함.
- 분량: 인스타 릴스 30초 이내(75~90단어) / 유튜브 1분 이내(150~180단어) /
  네이버 블로그 3000자 이내.
- 경기 데이터가 불확실하면 추측하지 말고 "확인 필요"로 표시한다.

## 데이터 소스

- 경기 결과/순위: football-data.org API (무료 티어), TheSportsDB
- 평점/MOM: 여러 소스를 참고만 하고, 반드시 자체 판단·재해석한 값을 근거 1줄과 함께 기록
  (특정 사이트 평점 그대로 복사 금지)

## 폴더 구조

```
data/matches/       # 경기별 원본 데이터 (JSON, /collect-data가 생성)
data/standings/     # 리그 순위 데이터
data/ratings/       # 평점/MOM 데이터
output/instagram/[날짜]/   # /write-instagram 결과물
output/naverblog/[날짜]/   # /write-blog 결과물
output/youtube/[날짜]/     # /write-youtube 결과물
scripts/            # 데이터 수집·그래픽·TTS 스크립트
templates/          # 플랫폼별 대본 템플릿
```

## 자동화 범위와 사람 확인 지점

- 1~4단계(데이터 정리, 대본, 자막, 캡션)와 6단계(게시 문구)는 슬래시 커맨드로 완전 자동화한다.
- 5단계(영상 렌더링)는 스크립트로 자동화하되, **실제 SNS 업로드는 사람이 최종 확인 후 수동으로 진행**한다.
  (계정 정지 리스크 + 오탈자/사실관계 체크 목적)

## 슬래시 커맨드

- `/collect-data` — 최근 경기 데이터를 수집해 `data/matches/`에 JSON으로 저장
- `/write-instagram` — 인스타 릴스 30초 대본 + 캡션/해시태그 작성
- `/write-blog` — 네이버 블로그 포스팅 작성
- `/write-youtube` — 유튜브 1분 영상 대본 + SRT 자막 작성

## 매일 자동 체크 & 발행 주기 정책

- 매일 1회(오전 9시 KST) `data/matches/`의 모든 경기 JSON을 확인한다.
- **게임데이 기준 최소 1일, 목표 2일 이내**에 리뷰를 작성한다 (당일 발행 금지 —
  통계·평점이 안정화되기 전에 서두르지 않는다).
- 처리 순서:
  1. `goalEvents`/`cardEvents`가 없는 파일은 `scripts/fetch_match_events.py`로 병합을 시도한다.
     (이 환경의 네트워크 정책상 API 호출이 막힐 수 있음 — 실패하면 "데이터 미확보"로
     남기고 다음 날 다시 시도한다. 추측으로 채우지 않는다.)
  2. `goalEvents`/`cardEvents`가 확보된 경기 중 `mom`/`rating`이 없는 파일은
     `/collect-data` 로직으로 채운다.
  3. 위 데이터가 모두 확보되고 게임데이 기준 1일 이상 지난 경기에 대해,
     아직 생성되지 않은 콘텐츠가 있으면 **인스타그램 + 네이버 블로그 + 유튜브
     3종을 한 번에 생성**한다 (`/write-instagram`, `/write-blog`, `/write-youtube`).
  4. 이미 `output/` 아래에 해당 경기·플랫폼 결과물이 있으면 재생성하지 않는다(멱등).
- 생성/병합된 파일은 커밋 후 원격 브랜치에 푸시한다.

## 매 게시물 발행 전 체크리스트

- [ ] 사용한 영상/이미지가 라이선스 프리이거나 공식 채널 임베드 방식인가?
- [ ] 인신공격성 표현 없는지 대본 재검수
- [ ] 플랫폼별 분량 규정 준수 (릴스 30초 / 유튜브 1분 / 블로그 3000자)
- [ ] MOM·평점·순위 변동 3종 모두 포함됐는지 확인
- [ ] 업로드 전 최종 사람 확인
